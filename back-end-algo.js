import { url } from "/server.js";
import { data } from "/server.js" assert { type: 'json' };;

//TODO this will be imported to index to be used as a button
console.log(url);
console.log(data); // should not error lol

//TODO: action items in CLI + externally
//Connect to postgres database
// see: https://docs.2bttns.com/reference/command-line-interface-cli/usage#configuration-values


